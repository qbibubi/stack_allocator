> Was it always this simple... It is an arena allocator, right?

# stack_allocator

Stack allocator is one of the simplest memory allocators available. Essentially this is a linear memory allocator that works perfectly for memory management of stack frames. The implementation itself could be flawed however the idea is there.
